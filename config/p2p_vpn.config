# Peer-to-Peer OpenVPN configuration (static key)
dev tun
ifconfig 10.8.0.1 10.8.0.2
secret static.key
port 1194
proto udp
# (Use `sudo openvpn --config p2p_vpn.conf` on one peer, and add `--remote <peer_ip>` on the other peer)
